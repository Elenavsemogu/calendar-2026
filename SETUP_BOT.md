# 🤖 Настройка Telegram бота (5 простых шагов)

**Все данные уже в коде! Просто копируй и вставляй.**

---

## Шаг 1: Открой Google Apps Script (1 минута)

1. Открой: https://script.google.com/
2. Нажми **"+ Новый проект"**
3. Назови проект: **"Secret Room Bot"**

---

## Шаг 2: Вставь код бота (30 секунд)

1. Открой файл **`telegram-bot.js`** из этого проекта
2. **Скопируй ВЕСЬ код** (Ctrl+A → Ctrl+C)
3. В Google Apps Script:
   - Удали весь текст (который там по умолчанию)
   - **Вставь код бота** (Ctrl+V)
4. **Важно!** Найди строку 16 и **замени** `CALENDAR_URL`:
   ```javascript
   CALENDAR_URL: 'https://твой-домен.github.io/calendar-2026/'
   ```
   Вставь свой домен (где будет хоститься календарь)

5. **Сохрани**: Ctrl+S (или File → Save)

---

## Шаг 3: Deploy как Web App (2 минуты)

1. Нажми **Deploy** (справа вверху) → **New deployment**
2. Нажми на иконку **⚙️ шестеренки** (Select type) → выбери **Web app**
3. Настрой:
   - **Description**: "Secret Room Bot v1"
   - **Execute as**: **Me** (твой email)
   - **Who has access**: **Anyone**
4. Нажми **Deploy**
5. Разрешения:
   - Нажми **Authorize access**
   - Выбери свой аккаунт
   - **Advanced** → **Go to "Secret Room Bot"**
   - Нажми **Allow**
6. **СКОПИРУЙ URL!** (выглядит как `https://script.google.com/macros/s/.../exec`)

---

## Шаг 4: Настрой Webhook (1 минута)

1. В Google Apps Script:
   - Найди функцию `setWebhook` (строка ~197)
   - Найди строку: `const webAppUrl = 'YOUR_WEB_APP_URL';`
   - **Вставь свой URL** (который скопировал выше)
   
   Было:
   ```javascript
   const webAppUrl = 'YOUR_WEB_APP_URL';
   ```
   
   Стало:
   ```javascript
   const webAppUrl = 'https://script.google.com/macros/s/ABC123.../exec';
   ```

2. **Сохрани** (Ctrl+S)
3. Выбери функцию **`setWebhook`** (вверху, в выпадающем списке)
4. Нажми **Run** (▶️)
5. Проверь **Execution log** (View → Execution log):
   - Должно быть: `"ok": true`
   - Если ошибка - проверь что URL правильный

---

## Шаг 5: Протестируй! (30 секунд)

1. Открой Telegram
2. Напиши боту: **@calendarreg_bot**
3. Нажми **Start** (или `/start`)
4. Должен прийти ответ с кнопкой!

**Если не подписан на канал:**
- Покажет кнопки "Подписаться" + "Проверить подписку"

**Если подписан:**
- Покажет кнопку "🗓 Открыть календарь" с персональной ссылкой!

---

## ✅ Готово!

Теперь:
- **Запуш код на GitHub** (уже готово, просто обнови сайт)
- **Тестируй бота** - открывай календарь через него
- **Делись ссылкой** `t.me/calendarreg_bot` в канале/рекламе

---

## 📊 Где смотреть пользователей?

В твоей Google Таблице (`1kwiWTnsfaxy-iNA9rXTHeMKalRS4Q42mgsezzTQLZJY`) появится новый лист **"Bot Users"** с данными:
- Telegram ID
- Имя/Фамилия
- Username
- Когда зашел

---

## 🐛 Если что-то не работает

### Бот не отвечает
- Проверь что webhook установлен: запусти функцию `getWebhookInfo()` в Apps Script
- Должно быть: `"url": "https://script.google.com/macros/s/.../exec"`
- Если `"url": ""` - webhook не установлен, повтори Шаг 4

### "Username invalid" в боте
- Проверь что бот добавлен в канал @secreetroommedia как админ
- Права: только "View channel statistics"

### Сайт не открывается с токеном
- Проверь что в `telegram-bot.js` правильный `CALENDAR_URL`
- Проверь что сайт задеплоен на этот домен

---

## 🎉 Как работает система:

1. Пользователь пишет боту
2. Бот проверяет подписку на канал
3. Если подписан → генерирует токен + дает ссылку на сайт
4. Сайт принимает токен из URL → сохраняет в localStorage
5. Доступ открыт! ✅

**Преимущества:**
- ✅ Всего 2 клика для пользователя
- ✅ Никаких форм и подтверждений
- ✅ Гарантия подписки на канал
- ✅ Все данные в Google Sheets
